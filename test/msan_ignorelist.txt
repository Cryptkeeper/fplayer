# CR_parse operates via cJSON, which is likely not compiled with MemorySanitizer, so we exclude it from the test.
fun:CR_parse
# CMap_lookup requires data initialized by CR_parse, so we exclude it from the test by association.
fun:CMap_lookup
